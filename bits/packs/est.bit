/*
    est.bit - Embedded Security Transport (SSL) package for Bit
 */

Bit.pack({
    est: {
        description: 'Embedded Security Transport'
        path: probe('src/deps/est/estLib.c'),
        libraries: [ 'est' ],
        conflicts: [ 'matrixssl', 'openssl', 'nanossl' ],
        requires:  [ 'ssl' ],
    },
})
