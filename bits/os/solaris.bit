/*
    solaris.bit -- Solaris Settings
 */

Bit.load({
    blend: [    
        'unix.bit',
        'gcc.bit',
    ],
    defaults: {
        '+libraries': [
            'lxnet', 
            'rt', 
            'socket',
        ],
    },
})

